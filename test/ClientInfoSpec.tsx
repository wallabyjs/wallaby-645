/// <reference path="../typings/globals/mocha/index.d.ts" />
import { mount } from 'enzyme'
import { expect } from 'chai'

describe('ClientInfo', () => {
    it('should render', () => {

        // const wrapper = mount(<ClientInfo/>, { attachTo: document.body })

        // expect(wrapper.find('h2').length).toBe(1)
        expect(1).to.equal(1)
    })
})
